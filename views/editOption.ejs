<html>
<head>
<title>Edit an Øption || Ønlíné Votíng Plàtform</title>
<meta name="csrf-token" content="<%= csrf %>" />
<meta name="viewport" content="width=device-width, initial-scale=1.0" />
<script src="https://cdn.tailwindcss.com"></script>
<link
rel="stylesheet"
href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.2.1/css/all.min.css"
integrity="sha512-MV7K8+y+gLIBoVD59lQIYicR65iaqukzvf/nwasF0nqhPay5w/9lJmVM2hMDcnK1OnMGCdVK+iQrJ7lzPJQd1w=="
crossorigin="anonymous"
referrerpolicy="no-referrer"
/>
</head>
<body
class="bg-gradient-to-tl from-blue-300 to-green-600 min-h-screen w-full"
>
<div class="flex justify-center text-red-1000">
<%- include ('updatedHeader', {username: username}) %>
</div>
<div class="flex flex-col justify-center items-center p-4">
<div class="flex flex-col justify-center md:w-2/5 w-full">
<hr />
<div class="pt-6 flex justify-between">
<a
href="/election/<%= election.id %>"
class="font-bold text-4xl hover:underline"
><%= election.name %></a
>
</div>
// here we using update option
<!-- Update Option -->
<div class="pt-6">
<p class="text-blue-600 text-2xl">EDIT OPTION</p>
<form
method="post"
action="/election/<%= election.id %>/question/<%= question.id %>/option/<%= option.id %>/update"
class="flex flex-col gap-4 pt-4"
>
<input type="hidden" name="_csrf" value="<%= csrf %>" />
<div class="flex flex-col w-full">
<input
type="text"
name="value"
value="<%= option.value %>"
placeholder="Value"
class="border-2 border-black p-2 rounded-lg"
/>
</div>
//putting button
<div class="flex justify-center items-center w-full gap-4">
<button
type="submit"
class="bg-green-500 transition duration-200 border-2 border-black p-2 rounded-lg hover:bg-green-700 px-6"
>
UPDATE
</button>
// putting cancel  
<a
href="/election/<%= election.id %>/question/<%= question.id %>"
class="bg-blue-500 transition duration-200 border-2 border-black p-2 rounded-lg hover:bg-gray-700 px-6"
>
CANCEL
</a>
</div>
</form>
// displaying flash message
<!-- flash message -->
<div class="flex flex-col justify-center items-center gap-3 pt-4">
<% if (messages && messages.error && messages.error.length > 0) { %>
<% for(var i=0; i<messages.error.length; i++) {%>
<div class="bg-blue-400 text-blue-1000 font-semibold p-2 rounded-lg shadow"> <%= messages.error[i] %></div>
<% } %>
<% } %>
</div>
</div>
</div>
</div>
</body>
</html>
