<html>
  <head>
    <title>SignUp</title>
    <meta name="csrf-token" content="<%= csrf %>" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-gradient-to-tl from-blue-300 to-green-600 min-h-screen w-full">
    <div class="flex justify-center"><%- include ('header') %></div>
    <div class="flex flex-col justify-center items-center p-4">
      <div class="flex flex-col justify-center md:w-1/5 w-full">
        <h1 class="text-2xl flex justify-center font-semibold">SignUp</h1>
        <form action="/users" method="post" class="flex flex-col gap-4 pt-6">
          <input type="hidden" name="_csrf" value="<%= csrf %>">
          <div class="flex flex-col">
            <label for="name">NAME</label>
            <input
              type="text"
              name="name"
              class="border-2 border-black p-2 rounded-lg"
            />
          </div>
          <div class="flex flex-col">
            <label for="email">EMAIL</label>
            <input
              type="text"
              name="email"
              class="border-2 border-black p-2 rounded-lg"
            />
          </div>
          <div class="flex flex-col">
            <label for="password">PASSWORD</label>
            <input
              type="password"
              name="password"
              class="border-2 border-black p-2 rounded-lg"
            />
          </div>
          <button
            type="submit"
            class="bg-red-700 border-2 border-red-900 flex justify-center hover:bg-red-900 text-white font-bold p-1 rounded-lg w-full mt-8"
          >
            SIGN-Up
          </button>
        </form>

        <!-- flash message -->
        <div class="flex flex-col justify-center items-center gap-3 ">
          <% if (messages && messages.error && messages.error.length > 0) { %>
            <% for(var i=0; i<messages.error.length; i++) {%>
              <div class="bg-red-400 text-red-1000 font-semibold p-2 rounded-lg shadow"> <%= messages.error[i] %></div>
            <% } %>
          <% } %>
        </div>

        <div class="flex flex-col justify-center pt-4 items-center">
          <p>Account exist already?</p>
          <a
            class="bg-red-200 border-2 px-8 border-black hover:bg-blue-200 p-1 rounded-lg"
            href="/login"
            >Log-In</a
          >
        </div>
      </div>
    </div>
  </body>
</html>
